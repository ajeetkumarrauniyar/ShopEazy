{
  "name": "omniledger",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "build": "turbo run build",
    "test": "turbo run test",
    "db:generate": "cd packages/db && npm run prisma:generate",
    "db:migrate": "cd packages/db && npm run prisma:migrate"
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^8.40.0",
    "prettier": "^3.0.0",
    "turbo": "^2.4.4",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.9.2",
  "dependencies": {
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.487.0",
    "tailwind-merge": "^3.1.0",
    "tw-animate-css": "^1.2.5"
  }
}
